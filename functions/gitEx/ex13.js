const pr = require('prompt-sync')();

function solveLinEquation(){
    let a = Number(pr('enter a : '));
    let b = Number(pr('enter b : '));
    let c = Number(pr('enter c : '));

    console.log(`solving equation ${a}x + ${b}y + ${c} = 0 .`);

    let x = (-b * ())
}